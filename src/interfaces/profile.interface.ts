export interface ProfileRes {
  accountType: string;
  created: string;
  id: string;
  phone: string;
  smsSubscribtion: boolean;
}

export interface AdminProfileRes {
  id: string;
  login: string;
  created: string;
}
